{
  "name": "dev",
  "version": "1.8.1",
  "description": "Provides development workflows, PR-writing guidance, code-craft preferences, runtime verification, the guided `crtr dev tutorial` onboarding walkthrough, and the bare `dev` dispatcher for Grove-backed projects — routes `dev grove …` to grove, repo-defined `.dev/<branch>` executables, and everything else to `grove dev`; `crtr dev` is a native extensible branch for repository-generated command fragments, with `crtr dev grove …` forwarded to Grove.",
  "commands": ".crouter-plugin/commands.json",
  "transport": {
    "kind": "exec",
    "executable": "bin/crtr-dev.mjs"
  },
  "requires": {
    "grove": "Install Grove, then run `grove setup` in the source repository."
  },
  "kinds": {
    "spec/requirements": {
      "whenToUse": "Derive testable EARS requirements from a finished, approved design — in isolation, from the rendered design text alone.",
      "model": "medium"
    }
  },
  "bin": {
    "dev": "bin/dev"
  },
  "source": "https://github.com/crouton-labs/crouter-official-marketplace",
  "owner": {
    "name": "Silas Rhyneer",
    "email": "rhyneer.silas@gmail.com"
  }
}
